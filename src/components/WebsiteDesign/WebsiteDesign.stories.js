import React from 'react';
import { storiesOf } from '@storybook/react';
import WebsiteDesign from './WebsiteDesign.jsx';

storiesOf('WebsiteDesign', module).add('WebsiteDesign', () => (
    <WebsiteDesign />
  ));